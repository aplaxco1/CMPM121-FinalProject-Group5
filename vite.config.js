export default {
  // eslint-disable-next-line no-undef
  base: process.env.REPO_NAME || "/repo-name/", // comment out for Capacitor
  // uncomment for Capacitor
  /*
  base: process.env.REPO_NAME || "/",
  
  build: {
    chunkSizeWarningLimit: 2000,
  },
  */ 
};
